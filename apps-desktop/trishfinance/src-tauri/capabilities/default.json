{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "TrishFinance — core + process + updater + opener (13-09)",
  "windows": [
    "main"
  ],
  "permissions": [
    "core:default",
    "process:default",
    "process:allow-restart",
    "updater:default",
    "opener:default"
  ]
}
