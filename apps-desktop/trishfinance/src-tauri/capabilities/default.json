{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "QuanLy Pro chỉ cần core permissions — không fs/shell ngoài IPC.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
