{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "TrishType cần dialog (open/save text/code files), opener (mở file folder bằng app default OS), HTTP fetch qua reqwest cho update check + Firebase Auth.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:default",
    "dialog:allow-open",
    "dialog:allow-save",
    "opener:default",
    "opener:allow-open-path",
    "opener:allow-open-url"
  ]
}
