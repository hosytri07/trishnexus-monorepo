{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "TrishCheck chỉ cần core permissions — KHÔNG fs, KHÔNG shell, KHÔNG network ngoài IPC.",
  "windows": [
    "main"
  ],
  "permissions": [
    "core:default",
    "updater:default",
    "process:default",
    "process:allow-restart"
  ]
}
