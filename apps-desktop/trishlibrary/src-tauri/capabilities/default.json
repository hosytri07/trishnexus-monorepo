{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "TrishLibrary cần dialog (pick folder scan + export/import JSON) và opener (mở file bằng app mặc định của OS). Store JSON + folder scan đều qua Rust std::fs — không cần fs plugin. Không shell, không http.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:default",
    "dialog:allow-open",
    "dialog:allow-save",
    "opener:default",
    "opener:allow-open-path"
  ]
}
