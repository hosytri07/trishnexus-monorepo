{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "TrishLibrary 3.0 — capabilities cho 3 module: dialog (pick folder/file), opener (mở file/URL), global-shortcut (Ctrl+Shift+N quick capture), notification (deadline reminder). HTTP fetch + scan folder qua Rust commands.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:default",
    "dialog:allow-open",
    "dialog:allow-save",
    "opener:default",
    "opener:allow-open-path",
    "opener:allow-open-url",
    "global-shortcut:default",
    "notification:default"
  ]
}
