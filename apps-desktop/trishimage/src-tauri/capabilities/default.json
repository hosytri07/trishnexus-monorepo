{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "TrishImage cần dialog để pick folder. Scan qua Rust walkdir + kamadak-exif + imagesize — không cần fs plugin (Rust tự đọc qua std::fs).",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:default",
    "dialog:allow-open"
  ]
}
