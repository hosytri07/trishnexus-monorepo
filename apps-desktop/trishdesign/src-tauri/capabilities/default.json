{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "TrishDesign cần dialog (open/save design-token JSON) + opener (mở file export & URL OAuth Google). Phase 14.4.10 thêm opener:allow-open-url cho Google Loopback OAuth flow.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:default",
    "dialog:allow-open",
    "dialog:allow-save",
    "opener:default",
    "opener:allow-open-path",
    "opener:allow-open-url",
    "opener:allow-default-urls"
  ]
}
