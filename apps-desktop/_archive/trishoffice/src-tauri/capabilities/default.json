{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "TrishOffice permissions: dialog (file picker) + opener + process.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:default",
    "opener:default",
    "process:default",
    "process:allow-restart"
  ]
}
