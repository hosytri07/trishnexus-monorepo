/**
 * Barrel — web (Next.js) implementations của @trishteam/adapters.
 * Phase 14.1 (2026-04-23).
 */

export { useNextRouterAdapter } from './next-router';
export { createWebStorageAdapter, webStorage } from './web-storage';
export { createWebClipboardAdapter, webClipboard } from './web-clipboard';
