{
  "name": "trishteam-firebase-scripts",
  "version": "0.1.0",
  "private": true,
  "description": "Admin SDK scripts: seed admin role, release reset.",
  "scripts": {
    "seed:admin": "ts-node seed-admin.ts",
    "release:reset": "ts-node reset-release.ts"
  },
  "dependencies": {
    "firebase-admin": "^12.1.0"
  },
  "devDependencies": {
    "@types/node": "^20.14.2",
    "ts-node": "^10.9.2",
    "typescript": "^5.4.5"
  }
}
